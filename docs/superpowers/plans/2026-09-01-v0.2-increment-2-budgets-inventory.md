# V0.2 Increment 2 · Budgets + Inventory Implementation Plan

> **For agentic workers:** implement task-by-task under the Master Agent Loop v0.2 (test first, evidence before status). Steps use checkbox (`- [ ]`) syntax. Increment 1 patterns (fan-out sync, tenant constraints, append-only ledgers, capability gates, offline-first repositories, lifecycle E2E) are the template — reuse, do not reinvent.

**Goal:** Close the Workshop Core money-and-parts loop: versioned quotes (labor, parts, taxes, discounts) with partial authorization evidence, and an inventory ledger (products, append-only movements, derived stock) with reservation/consumption tied to service orders.

**Scope (FR coverage):**

- `FR-SVC-007` quote with labor lines, part lines, taxes and discounts; deterministic totals.
- `FR-SVC-008` quote versioning: revisions never overwrite; supersession is explicit.
- `FR-SVC-009` authorization / rejection / partial authorization with recorded evidence (actor, timestamp, per-line decisions).
- `FR-SVC-011` reserve and consume inventory against order task lines.
- `FR-INV-001` (basic) products with SKU and unit; `FR-INV-003` every stock change is an `InventoryMovement`; `FR-INV-004` stock is derived from the ledger, never stored as mutable truth.

**Out of scope here:** purchase orders (FR-INV-008), physical counts (FR-INV-010), providers (FR-INV-007), POS (FR-POS-*), payments, attachments/photos, QR.

**Key domain decisions to enforce:**

- **Ledger immutability:** `inventory_movements` is append-only (like `service_order_events`); corrections are compensating movements, never updates. Stock = `SUM(quantity)` filtered by product/location; there is no mutable stock column.
- **Reservation discipline:** `RESERVE` movements hold stock (quantity negative against available); `RELEASE` compensates a reservation; `CONSUME` finalizes it. Reservations reference the order; consumption references both order and reservation movement when applicable.
- **Quote versioning:** a quote belongs to an order; revisions append with `version = previous + 1` and `superseded_at` on the predecessor; totals are computed server-side from lines (never trusted from the client); money uses integer minor units (cents) with explicit currency, no floats.
- **Authorization evidence:** order gains `authorization` state data (decision, actor, timestamp, per-line overrides for partials); `authorize`/`reject`/`authorize_partial` are explicit actions with audit events in the order timeline.
- Sync: `quote` and (server-only) inventory ride the mutation pipeline where offline-first makes sense; **inventory movements are server-authoritative** (online commands like the transitions endpoint) — stock truth never depends on offline queues. Products sync offline-first like customers.

## Global Constraints

- Same as Increment 1 (AGPL-3.0-only, Offline/Local First where applicable, UUIDv7 ids, deny-by-default capabilities `inventory.*`/`quotes.*`, idempotent sync, PostgreSQL canonical/SQLite surrogate, TDD, ADR notes in commit bodies for material decisions).

---

### Task 1: Inventory domain + ledger (server)

**Files:**

- Create: `services/platform/src/cycling_workshop/inventory/{__init__,domain,models,repository,schemas,router}.py`
- Test: `services/platform/tests/inventory/test_inventory_domain.py`, `test_inventory_repository.py`

**Interfaces:**

- `Product.create(sku, name, unit, organization_id, location_id)` with normalization + blank-sku rejection.
- `InventoryMovement.record(product_id, organization_id, location_id, kind: RESERVE|RELEASE|CONSUME|ADJUST, quantity, order_id?, reference_movement_id?, actor_id, note?)` — append-only; RELEASE/CONSUME must reference their RESERVE; quantities are signed integers (RESERVE −n, RELEASE +n, CONSUME −n, ADJUST ±n).
- `available_quantity(session, product_id, location scope)` = SUM of movements.

- [ ] Failing domain tests: movement matrix (signs, references, unknown kind rejected), stock derivation, double-release rejected.
- [ ] Repository tenant-scoped; ledger `list_by_order`, `list_by_product`.
- [ ] Commit `feat(inventory): product and append-only stock ledger`.

### Task 2: Migration 0006 (products, inventory_movements)

- Create: `services/platform/migrations/versions/0006_inventory.py`; extend registration + migration tests.
- [ ] Empty PostgreSQL 18.4 → head green (local Docker evidence).
- [ ] Commit `feat(migrations): 0006 inventory tables`.

### Task 3: Inventory API (server-authoritative commands)

- `POST /api/v1/inventory/products`, `GET /api/v1/inventory/products`, `POST /api/v1/inventory/movements` (RESERVE/RELEASE/CONSUME/ADJUST with order linkage), `GET /api/v1/inventory/products/{id}/stock` (derived).
- Capabilities: `inventory.read`, `inventory.write`; negative tests (401/403/404/409 for invalid references).
- [ ] Commit `feat(api): inventory commands and derived stock`.

### Task 4: Quote domain + versioning (server)

- `Quote.for_order(order_id, lines[], currency)` → version 1; `revise(quote, lines[])` → version+1, predecessor superseded; totals computed from typed lines: `labor(minutes × rate)`, `part(product, quantity × unit_price)`, `tax(rate, base subset)`, `discount(amount, scope)`; integer minor units.
- [ ] Totals property tests (ordering independence, rounding at line boundaries).
- [ ] Commit `feat(quotes): versioned quote aggregate with deterministic totals`.

### Task 5: Quote authorization with evidence (server)

- `authorize / reject / authorize_partial(lines subset with overrides)` as explicit actions emitting timeline events; partial records per-line evidence; order state integrates with the existing machine (AUTHORIZATION data, not new states).
- [ ] Commit `feat(quotes): authorization decisions with evidence`.

### Task 6: Migration 0007 (quotes, quote_lines, quote_authorizations)

- [ ] PostgreSQL evidence; registration tests.
- [ ] Commit `feat(migrations): 0007 quote tables`.

### Task 7: Sync fan-out for products + quotes

- Envelope widens to `product` and `quote`; apply with base-version discipline; quote revisions via update mutations carrying new lines; conflict on stale revision (permanent).
- [ ] ADR note: concurrent quote revision = permanent conflict (last-writer-wins is unacceptable for money).
- [ ] Commit `feat(sync): products and quotes ride the pipeline`.

### Task 8: REST + contracts for quotes

- `POST/GET /api/v1/service-orders/{id}/quotes`, `POST /api/v1/quotes/{id}/authorize|reject|authorize-partial`; regenerate OpenAPI + TS client.
- [ ] Commit `feat(api): quote endpoints + regenerated contracts`.

### Task 9: Local stores (Dexie v4) + offline quote drafting

- Schema v4: `products`, `quotes` (drafts offline with base_version on revise); repositories reuse the envelope.
- [ ] Commit `feat(web): offline quote drafts (schema v4)`.

### Task 10: UI — quote editor + inventory panel

- Order detail gains "Presupuesto" editor (labor/part lines, totals preview) and stock panel with reserve/consume actions (online commands).
- [ ] Commit `feat(web): quote editor and inventory panel`.

### Task 11: E2E — quote lifecycle + reservation

- Draft offline → sync → authorize partial online → supersede → conflict path for concurrent revision → inventory reserve/consume reflected in derived stock.
- [ ] Commit `test(e2e): quote and inventory lifecycle`.

### Task 12: Increment qualification

- Verifier/state/README + plan checkboxes; hosted green artifact.
- [ ] Commit `chore: qualify V0.2 increment 2`.

---

## Execution notes

- One PR per task (or tight pairs), never one giant PR; every PR carries the full local ladder evidence in its body.
- Money as integer minor units everywhere — ADR-level constraint, reject any float in totals.
- Inventory commands remain online (server-authoritative); document the offline boundary in the UI copy when stock can't be checked offline.
