# V0.1 Checkpoint D Runtime Qualification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert the remaining V0.1 environment-dependent gates into reproducible runtime evidence where this execution environment permits, without weakening PostgreSQL 18.4, browser, accessibility, security, or device qualification requirements.

**Architecture:** Keep the existing V0.1 implementation unchanged unless runtime qualification exposes a concrete defect. Treat local verification, temporary remote qualification infrastructure, and target-device qualification as distinct evidence classes. Temporary external resources must be isolated from production data and removed after evidence collection.

**Tech Stack:** Python 3.13, FastAPI, SQLAlchemy 2, Alembic, PostgreSQL 18.4, React 19, TypeScript, Vite, Dexie/IndexedDB, Playwright, axe-core, GitHub Actions, uv, pnpm.

## Global Constraints

- License remains `AGPL-3.0-only`.
- Branch remains `bootstrap/v0.1`; do not implement on main/master.
- PostgreSQL 18.4 is the canonical V0.1 server-engine qualification target.
- IndexedDB/Dexie browser evidence must not be replaced by a non-browser surrogate.
- Android and Windows storage qualification remains mandatory before closing `R01-T035`.
- No gate is marked complete from configuration alone; fresh execution evidence is required.
- Temporary external infrastructure must contain no production or personal data and must be deleted after qualification.
- Runtime defects discovered during qualification are fixed with RED → GREEN TDD before continuing.

---

### Task 1: Restore repository-contract integrity

**Files:**
- Modify: `tests/repository/test_repository_contract.py`
- Create: `packages/plugin-sdk/package.json`
- Create: `packages/plugin-sdk/src/index.ts`

**Interfaces:**
- Produces a versioned `@ocwp/plugin-sdk` workspace boundary required by Phase 5 and the repository verifier.

- [x] Add a failing contract assertion that the plugin SDK has a package manifest and public entrypoint.
- [x] Run the focused repository-contract test and confirm RED because the workspace is absent from Git.
- [x] Add the minimal private package manifest and empty public entrypoint. Do not invent plugin runtime APIs in V0.1.
- [x] Re-run repository-contract tests and `scripts/ci/verify_repository.py`; require GREEN.
- [x] Run the full Python suite to prove no regression.
- [x] Commit with DCO sign-off.

### Task 2: Qualify canonical PostgreSQL migrations on an isolated PostgreSQL 18.4 service

**Files:**
- No production code changes unless the qualification exposes a reproducible defect.
- Evidence: `docs/10-spec-development/execution-state.yaml` in Task 4.

**Interfaces:**
- Consumes Alembic revisions `0001` through `0004`.
- Produces runtime evidence for `R01-T013` and `R01-T014` without retaining remote data.

- [x] Provision an isolated PostgreSQL 18.4 qualification database/branch with no production data.
- [x] Confirm `server_version` is 18.4 before applying schema.
- [x] Generate the canonical PostgreSQL SQL from Alembic using `alembic upgrade head --sql`.
- [x] Apply the generated schema only to the isolated qualification branch.
- [x] Verify Alembic head is `0004` and all V0.1 tables exist.
- [x] Insert and read a synthetic Organization → Location → Customer row to verify tenant foreign-key behavior.
- [x] Delete the qualification branch/project and verify no application tables were written to a parent/default database.

### Task 3: Re-probe browser and dependency qualification gates

**Files:**
- No code changes unless a reproducible runtime defect is found.

**Interfaces:**
- Produces fresh blocker/pass evidence for pnpm, uv locks, PWA build, IndexedDB, Playwright and axe gates.

- [x] Probe executable pnpm/Corepack and record the exact result.
- [x] Probe `uv lock --offline` and record the exact missing-cache or success result.
- [x] Run `scripts/benchmarks/run_indexeddb_native_benchmark.py` and record the exact browser preflight/benchmark result.
- [x] Run `scripts/ci/verify_v01.py --allow-blocked` after Task 1 and require zero `fail` checks.
- [x] Keep browser/device gates blocked if Chromium navigation, package registry access, or target devices remain unavailable.

### Task 4: Record Checkpoint D evidence and package the exact commit

**Files:**
- Modify: `docs/10-spec-development/execution-state.yaml`
- Create outside repository: Checkpoint D Markdown/DOCX report, verification JSON, source ZIP, Git bundle, SHA256SUMS.

**Interfaces:**
- Produces the resumable state and downloadable checkpoint artifacts.

- [x] Update execution state with the repository-contract fix and PostgreSQL 18.4 temporary-branch evidence.
- [x] Preserve all unresolved browser, lockfile, scanner, Docker and target-device gates as pending/blocked.
- [x] Run fresh full verification on the exact final commit.
- [x] Verify every commit in history contains `Signed-off-by`.
- [ ] Create source ZIP and Git bundle from the exact commit and verify both archives.
- [ ] Generate editable Markdown and DOCX checkpoint reports and inspect the rendered DOCX before delivery.
